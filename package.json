{
  "name": "qsentinel",
  "private": true,
  "description": "QSENTINEL dashboard (Vite) with FastAPI serverless backend",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "dev": "npm run dev --prefix frontend"
  }
}
